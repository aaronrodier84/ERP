%tr{ :class => highlight_helper(user, current_user) }
  %td
    = link_to edit_admin_user_path(user) do
      %span.icon-edit.icon-large
        Edit
  %td
    = user.full_name
  %td
    = user.email
  %td.text-left
    - user.zones.each do |zone|
      = zone_label(zone)
